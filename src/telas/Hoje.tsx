import { EstadoVazio } from '../visual/componentes'

/** Aba inicial e destino da notificação. Conteúdo real na Etapa 3. */
export function Hoje() {
  return (
    <EstadoVazio
      titulo="Nada para hoje"
      texto="Quando você cadastrar plantas, o que precisa de rega aparece aqui primeiro."
    />
  )
}
