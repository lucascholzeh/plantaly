import { EstadoVazio } from '../visual/componentes'

/** Grade mensal com histórico e previsão. Conteúdo real na Etapa 5. */
export function Calendario() {
  return (
    <EstadoVazio
      titulo="Sem registros neste mês"
      texto="O calendário mostra o que já aconteceu e o que está previsto."
    />
  )
}
